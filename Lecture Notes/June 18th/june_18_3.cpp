#include <iostream>
using namespace std;
class list ....

void merge(node *cu1, node *cu2, node *cu3) {
    
}

int main() {
    list l1,l2,l3;
    for loop fill randomly l1 and l2 
    merge them
    
    while (l1.gethead() && l2.gethead()) {
        // compare l1 to l2
        // delete from beginning the smaller
        // add at the end in l3
    }
        ??? if find out which list has __END_DECLS
        if (l1.gethead()==0) {
            while(l2.gethead() != 0)
        }
}